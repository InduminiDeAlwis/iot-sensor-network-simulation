package utils;

public class JSONUtil {
    public static String toJson(Object o) {
        // Very small placeholder; replace with a real JSON lib in real project
        return o == null ? "null" : o.toString();
    }
}
